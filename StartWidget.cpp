#include "StartWidget.h"

StartWidget::StartWidget(QWidget *parent) :
	QWidget(parent)
{
	setFixedSize(250, 126);
}

void StartWidget::paintEvent(QPaintEvent *)
{
//	QPainter painter(this);

//	painter.setBrush(QPixmap(":/images/menu-fond.png"));

//	SaveWidget::drawFrame(&painter, 250, 42);
//	FF8Text::drawTextArea(&painter, QPoint(12, 12), tr("Ouvrir..."));

//	painter.translate(0, 42);
//	SaveWidget::drawFrame(&painter, 250, 42);
//	FF8Text::drawTextArea(&painter, QPoint(12, 12), tr("Fente 1"));

//	painter.translate(0, 42);
//	SaveWidget::drawFrame(&painter, 250, 42);
//	FF8Text::drawTextArea(&painter, QPoint(12, 12), tr("Fente 2"));


//	painter.end();
}
